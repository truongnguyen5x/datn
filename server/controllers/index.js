const userController  = require('./user')
const tokenController = require('./token')
const templateTokenController = require('./template')
const dappController = require("./dapp")

module.exports = {
    userController,
    tokenController,
    templateTokenController,
    dappController
}