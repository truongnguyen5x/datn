import React from 'react'
import { connect } from "react-redux"

const ListToken = (props) => {
    return <React.Fragment>
        <h1>
           List Token for Admin  </h1>
       

    </React.Fragment>
}

export default  connect(null, {})(ListToken)