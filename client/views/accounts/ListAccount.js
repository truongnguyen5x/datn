import React from 'react'
import { connect } from "react-redux"

const ListAccount = (props) => {
    return <React.Fragment>
        <h1>
            List Account
        </h1>


    </React.Fragment>
}

export default connect(null, {})(ListAccount)